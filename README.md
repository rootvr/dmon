```console
help                  print help
all                   combo: clean, resolve deps and run dmon
build                 compile dmon
clean                 clean and remove dmon binary
run                   compile and run dmon
deps                  resolve dependencies
fmt                   format all
gen                   generate dmon docker image
rmi                   delete dmon docker image
```
